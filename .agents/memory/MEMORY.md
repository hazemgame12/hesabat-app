# Memory Index

- [esbuild externalized deps crash deploy](esbuild-externalized-deps.md) — api-server ships as one zero-dep bundle; any used pkg left in build.mjs `external` → ERR_MODULE_NOT_FOUND → 503 (dev hides it).
- [Hostinger env vars not persisting](hostinger-env-vars-not-persisting.md) — new Hostinger Deployments env vars vanish after Save+redeploy; blocks one-click social OAuth (app reads only process.env). Use support or DB-backed manual token.
- [Hesabat accounting SaaS mockups](hesabat-mockups.md) — visual-only mockups for an Egyptian cloud-accounting SaaS; financial demo data must reconcile across statements (auditor user).
- [Hesabat architecture](hesabat-architecture.md) — multi-tenant accounting app: scope all queries by companyId AND re-validate cross-row FKs (parentId) to caller's company; native scrypt+cookie auth; roles/permissions/invitations; QueryClient retry:false.
- [Hesabat opening balances](hesabat-opening-balances.md) — own-row rows (bank/inventory) must send+process zeros to reset; JE-rebuilt rows omit zeros; codegen body component must NOT be named `<operationId>Body`.
- [Hesabat journal entries](hesabat-journal-entries.md) — multi-currency (balance on base amounts), local-disk attachments (forced-download, cleanup all paths), exceljs import/export; GET /journal/export must register before /journal/:id.
- [Secrets in URL paths](secrets-in-url-paths.md) — tokens in URL path segments leak into request logs; redact them in the pino-http req serializer (query-string already stripped, path isn't).
- [Orval multipart codegen broken](orval-multipart-codegen.md) — declaring multipart/form-data requestBody generates broken Blob type + dup export; omit requestBody, upload via manual fetch+FormData.
- [Hesabat i18n](hesabat-i18n.md) — i18next ar(default)/en; sync html lang+dir → use Tailwind logical props (ms/ps/start/text-start) not dir="rtl"; group/tab by stable IDs, translate only for display.
- [Journal entryNo numbering race](entrynno-numbering-race.md) — per-company entryNo (max+1, no unique constraint) races under concurrent posting; call lockCompanyEntryNo(tx,companyId) before allocating.
- [Subsidiary-ledger parties](subsidiary-ledger-parties.md) — customers/suppliers store NO balance; auto-create a leaf account under a control group; derive balance from posted lines; advisory-lock child-code allocation.
- [Hesabat lock-order contract](hesabat-lock-ordering.md) — any tx taking BOTH a row FOR UPDATE and lockCompanyEntryNo must lock rows first (sorted), THEN the advisory lock; reverse order deadlocks approve vs payment.
- [Hesabat bank module](hesabat-bank-module.md) — bank/cash accts link to EXISTING leaf chart acct (not auto-created); movements post POSTED JEs; transfer = 2 linked rows + 1 JE; delete blocks if ANY transfer-group row cleared.
- [Hesabat payroll/advances concurrency](hesabat-payroll-advances-concurrency.md) — running-balance mutations (totalRepaid) must be in-tx under company lock + FOR UPDATE + SQL arithmetic; JE-backed statuses (settled) rejected in BOTH create+update.
- [Hesabat bank reconciliation](hesabat-bank-reconciliation.md) — canonical difference = statement − clearedBookBalance everywhere (persist on create/match/adjust/complete); report pairs adjustedStatementBalance with FULL book balance.
- [Hesabat Excel import/export](hesabat-excel-io.md) — raw routes (no codegen), mirror journal.ts; import all-or-nothing tx + company-scoped FK resolution; import=master-data only; shared ExcelToolbar + common.* i18n keys.
- [Audit log design](audit-log.md) — append-only (no write/delete route); safeAudit writes are best-effort post-commit and must never break the business tx; GET /audit is owner/manager only, company-scoped.
- [Fiscal Year close/period-lock](fiscal-year.md) — carry-forward = year-end CLOSING entry (cumulative-from-genesis, no opening entry); lock FY row + WHERE status='open' on close; per-calendar-year JV numbering via allocateEntryNo on EVERY mint path (incl. Excel import).
- [Auto entity codes](auto-entity-codes.md) — server-gen PREFIX-YEAR-NNNN via code_sequences; key on year LABEL not fiscal_years.id; COA+currency stay manual; never seed auto-format codes (desync→23505 stuck).
- [Hesabat returns & print](hesabat-returns-and-print.md) — credit/debit notes = invoice rows by kind (service-only v1, ref posted same-party source); chrome-less /print routes need their own client auth guard + error UI.
- [drizzle `= ANY(jsArray)` bug](drizzle-any-array-bug.md) — never filter a column by a JS array via `sql\`= ANY(${arr})\`` (Postgres "requires array" error); use `inArray(col, arr)`.
- [Bank movement pending/classify](bank-movement-pending-classify.md) — pending = journalEntryId IS NULL (derived, no status col); balance sums exclude pending; classify PATCH must keep movement↔JE in sync (clear counterpart → delete JE).
- [Bank movement ↔ payment linking](bank-movement-payment-link.md) — قبض/صرف derives from movement.DIRECTION not .type (in→collection/customer); link-payment replaces the JE; lock invoice rows sorted → lockCompanyEntryNo; capability=payments:create.
- [Hesabat pre-documents](hesabat-pre-documents.md) — quotation/purchase_order reuse invoicesTable + InvoiceEditor; approve=confirm (no JE), convert=copy lines to new invoice; InvoiceEditor Kind type must include all 4 kinds + isSalesSide helper.
- [Hesabat Period Lock](period-lock.md) — `isWriteBlocked` returns "period_locked"|"fiscal_closed"|false; 423 for locked, 400 for closed; `WRITE_BLOCK_MSG[reason]`; PATCH /company/period-lock; PeriodLockCard in fiscal-years.tsx.
- [VPS GitHub Actions Secrets](vps-deploy-secrets.md) — CI build passes but SSH deploy fails: VPS firewall blocks GitHub Actions IPs. Secrets ARE set (VPS_HOST/VPS_USER/VPS_SSH_KEY in GitHub production env). Real deploy = webhook → update-hg.sh.
- [GitHub webhook 401](webhook-rawbody-401.md) — express.json() drains body before HMAC → empty rawBody → 401 every delivery; use parser `verify` hook. Sign a test POST yourself: 200 vs GitHub 401 = secret mismatch, not code.
- [VPS HG website deployment](vps-hg-deploy.md) — real deploy = GitHub push → webhook (hg-audit.com/api/webhook/deploy, hook 645091245) → update-hg.sh; builds HG+API+Hesabat, pm2 restart; GitHub Actions SSH always fails (firewall); use 127.0.0.1 not localhost in nginx.
- [GitHub push from Replit](github-push-replit.md) — git push blocked by replit-git-askpass; GITHUB_TOKEN in bash only (not code_execution); use `pnpm run push-github` (scripts/push-github.py): Git Data API tree commit + auto-triggers deploy webhook.
- [VPS deploy stale frontend](vps-deploy-stale-frontend.md) — update-hg.sh must BUILD each frontend before copying; copy-only ships stale dist and self-updates via git reset so script changes need TWO triggers.
- [Hesabat vite PORT in production](hesabat-vite-port-production.md) — vite.config.ts threw on missing PORT even in production builds (NODE_ENV=production); fix: skip PORT check when isProduction; VPS update-hg.sh uses BASE_PATH=/ NODE_ENV=production without PORT so Hesabat build silently failed every deploy.
- [VPS migration FK ordering](vps-migration-fk-ordering.md) — migrations run alphabetically; `exit 1` on any failure aborts entire deploy (no build, no pm2 restart → VPS frozen). Fix: create a prerequisite migration that sorts BEFORE the failing one alphabetically (filename prefix trick: "c" < "p").
- [VPS Hesabat URL is subdomain](vps-hesabat-url.md) — Hesabat app lives at hesabat.hg-audit.com (not hg-audit.com/hesabat/); /hesabat/ on hg-audit.com is HG website SPA fallback. Always verify against the subdomain.
- [update-hg.sh CDN self-update replaced](update-hg-git-first.md) — GitHub raw CDN caches old script for minutes; fixed by git-reset-first then `HG_PULLED=1 exec bash $APP_DIR/scripts/update-hg.sh` (re-exec local file after reset, no CDN needed).
- [ensure-schema full coverage rule](ensure-schema-coverage.md) — every Drizzle table column must appear in ensure-schema; omitting invoices header (while covering invoice_lines) caused production outage: SELECT * failed, invoices+customers went blank.
- [Hesabat VPS nginx BASE_PATH](hesabat-vps-basepath.md) — nginx on VPS serves hesabat.hg-audit.com with assets at /assets/ (not /hesabat/assets/); always build Hesabat dist with BASE_PATH=/ for VPS; Replit dev uses BASE_PATH=/hesabat/ (artifact path).
- [PR merge does not auto-deploy](pr-merge-no-deploy.md) — GitHub PR merge via API does NOT trigger the VPS deploy webhook; only push-github.py (Git Data API + manual webhook call) does. After merging a PR, always manually trigger the webhook.
- [GitHub merge in main agent](github-merge-main-agent.md) — git fetch/checkout blocked in main agent; merge conflicts resolved via GitHub API: fetch files at branch SHA, write locally, build, create merge commit with two parents via Git Data API tree + commit endpoint.
- [Invoice posting dimensions gap](invoice-posting-dimensions.md) — DraftPostingLine only had costCenterId; projectId+branchId must be added to the type, the INSERT, AND every entryLines.push() call in invoices.ts.
- [VPS core schema missing — migrate-00 fix](vps-core-schema-missing.md) — hg_db had no Hesabat tables; drizzle-kit push --force fails silently on VPS; fix = migrate-00-core-schema.sql (all 52 tables, IF NOT EXISTS, FK order) in hostinger-deploy-sql/; name "00-" sorts before all existing migrations so it always runs first.
